// Add Color Pallete Modal

        jQuery(document).ready(function($) {
            $('.color-picker').wpColorPicker();
        });


// Start Facebook Button Margin Top

function clearSliderValueMarginTop() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_margin_top"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValueMarginTop(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValueMarginTop(value) {
    document.getElementById('sliderValueMarginTop').text = 'Margin Top ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('margin-top', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValueMarginTop').text('Margin Top ' + value);
            }
// End Facebook Button Margin Top



// Start Facebook Button Margin Right

function clearSliderValueMarginRight() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_margin_right"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValueMarginRight(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValueMarginRight(value) {
    document.getElementById('sliderValueMarginRight').textContent = 'Margin Right ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('margin-right', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValueMarginRight').text('Margin Right ' + value);
            }
// End Facebook Button Margin Right


// Start Facebook Button Margin Bottom

function clearSliderValueMarginBottom() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_margin_bottom"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValueMarginBottom(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValueMarginBottom(value) {
    document.getElementById('sliderValueMarginBottom').textContent = 'Margin Bottom ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('margin-bottom', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValueMarginBottom').text('Margin Bottom ' + value);
            }
// End Facebook Button Margin Bottom


// Start Facebook Button Margin Left

function clearSliderValueMarginLeft() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_margin_left"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValueMarginLeft(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValueMarginLeft(value) {
    document.getElementById('sliderValueMarginLeft').textContent = 'Margin Left ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('margin-left', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValueMarginLeft').text('Margin Left ' + value);
            }
// End Facebook Button Margin Left





// Add Clear Values for Padding Sliders in the Admin Dashboard Page

// Start Facebook Button Padding Top

function clearSliderValuePaddingTop() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_padding_top"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValuePaddingTop(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValuePaddingTop(value) {
    document.getElementById('sliderValuePaddingTop').text = 'Padding Top ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('padding-top', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValuePaddingTop').text('Padding Top ' + value);
            }
// End Facebook Button Padding Top


// Start Facebook Button Padding Right

function clearSliderValuePaddingRight() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_padding_right"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValuePaddingRight(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValuePaddingRight(value) {
    document.getElementById('sliderValuePaddingRight').text = 'Padding Right ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('padding-right', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValuePaddingRight').text('Padding Right ' + value);
            }
// End Facebook Button Padding Right


// Start Facebook Button Padding Bottom

function clearSliderValuePaddingRight() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_padding_bottom"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValuePaddingBottom(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValuePaddingBottom(value) {
    document.getElementById('sliderValuePaddingBottom').text = 'Padding Bottom ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('padding-bottom', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValuePaddingBottom').text('Padding Bottom ' + value);
            }
// End Facebook Button Padding Bottom


// Start Facebook Button Padding Left

function clearSliderValuePaddingLeft() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_container_padding_left"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValuePaddingLeft(0);
            }
            // Function to update the Facebook button font size
            function updateSliderValuePaddingLeft(value) {
    document.getElementById('sliderValuePaddingLeft').text = 'Padding Left ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('padding-left', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValuePaddingLeft').text('Padding Left ' + value);
            }
// End Facebook Button Padding Bottom





function clearSliderValueShareTextFontSize() {
  // Set the value of the range slider to its default or desired initial value
  document.querySelector('input[name="seobpsbfw_share_text_font_size"]').value = 18;

  // Call the function to update the slider value display
  updateSliderValueShareTextFontSize(18);
}


function clearSliderValueTextTransform() {
        // Set the value of the select menu to its default or desired initial value
        document.querySelector('select[name="seobpsbfw_share_container_box_text_transform"]').value = 'none';

        // Call the function to update the slider value display
        updateSliderValueTextTransform('none');
    }
    function updateSliderValueTextTransform(value) {
        document.getElementById('sliderValueTextTransform').textContent = value;
    }

function clearSliderValueTextFontFamily() {
        // Set the value of the select menu to its default or desired initial value
        document.querySelector('select[name="seobpsbfw_share_container_box_text_font_family"]').value = 'sans-serif';

        // Call the function to update the slider value display
        updateSliderValueTextFontFamily('sans-serif');
    }



// Social Media Buttons Clear Settings

function clearSliderValueFbShareButtonMargin() {
  // Set the value of the range slider to its default or desired initial value
  document.querySelector('input[name="seobpsbfw_fb_share_button_margin"]').value = 0;

  // Call the function to update the slider value display
  updateSliderValueFbShareButtonMargin(0);
}



function clearSliderValueFbShareButtonPaddingTop() {
  // Set the value of the range slider to its default or desired initial value
  document.querySelector('input[name="seobpsbfw_fb_share_button_padding_top"]').value = 0;

  // Call the function to update the slider value display
  updateSliderValueFbShareButtonPaddingTop(0);
}



            function clearSliderValueFbShareButtonFontSize() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_fb_share_button_font_size"]').value = 22;

              // Call the function to update the slider value display
              updateSliderValueFbShareButtonFontSize(22);
            }
            // Function to update the Facebook button font size
            function updateSliderValueFbShareButtonFontSize(value) {
    document.getElementById('sliderValueFbShareButtonFontSize').textContent = 'Font Size ' + value;
                // Update the font size of the Facebook button
                $('.facebook-button').css('font-size', value + 'px');
                
                // Update the text in the slider value display
                $('#sliderValueFbShareButtonFontSize').text('Font Size ' + value);
            }



    







            // Add Clear Values for Padding Sliders in the Admin Dashboard Page





            function clearSliderValueShareTextFontSize() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_share_text_font_size"]').value = 18;

              // Call the function to update the slider value display
              updateSliderValueShareTextFontSize(18);
            }


            function clearSliderValueTextTransform() {
                    // Set the value of the select menu to its default or desired initial value
                    document.querySelector('select[name="seobpsbfw_share_container_box_text_transform"]').value = 'none';

                    // Call the function to update the slider value display
                    updateSliderValueTextTransform('none');
                }
                function updateSliderValueTextTransform(value) {
                    document.getElementById('sliderValueTextTransform').textContent = value;
                }

            function clearSliderValueTextFontFamily() {
                    // Set the value of the select menu to its default or desired initial value
                    document.querySelector('select[name="seobpsbfw_share_container_box_text_font_family"]').value = 'sans-serif';

                    // Call the function to update the slider value display
                    updateSliderValueTextFontFamily('sans-serif');
                }



            // Social Media Buttons Clear Settings

            function clearSliderValueFbShareButtonMargin() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_fb_share_button_margin"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValueFbShareButtonMargin(0);
            }



            function clearSliderValueFbShareButtonPaddingTop() {
              // Set the value of the range slider to its default or desired initial value
              document.querySelector('input[name="seobpsbfw_fb_share_button_padding_top"]').value = 0;

              // Call the function to update the slider value display
              updateSliderValueFbShareButtonPaddingTop(0);
            }





            function clearSliderValueTextFontFamily() {
                    // Set the value of the select menu to its default or desired initial value
                    document.querySelector('select[name="seobpsbfw_share_container_box_text_font_family"]').value = 'none';

                    // Call the function to update the slider value display
                    updateSliderValueTextFontFamily('none');
                }
                function updateSliderValueTextFontFamily(value) {
                    document.getElementById('sliderValueTextFontFamily').textContent = value;
$('.column.share-column').css('font-family', value);
$('#sliderValueTextFontFamily').text('Font Family ' + value);
                }

